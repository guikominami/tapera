export interface ReferencesSection {
    title: string;
}

export interface ReferencesTextData {
    id: number;
    title: string;
    subtitle: string;
    column: number;
    frame: string;
    height: string;
}